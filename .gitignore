# Local Claude Code / editor tooling (not game content)
.claude/

# OS / editor cruft
Thumbs.db
.DS_Store
*.log

# Local snapshot of the original (pre-rename) art — kept on disk, not in the repo
assets_backup/
